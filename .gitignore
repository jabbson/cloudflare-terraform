# Terraform internals
.terraform/
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# State files
*.tfstate
*.tfstate.*

# Plan output files
tfplan
tfplan.json
plan.txt

# Credentials
.env

# Generated artifacts (regenerated by generate.sh — contain live infra data)
*.auto.tfvars.json
imports.tf

# Example templates for hand-managed extra records — committed to git
# (actual *.auto.tfvars.json files are gitignored; copy from .example and fill in)

# Hand-managed but contains real server IP
traefik.auto.tfvars

# Temp curl output files from generate.sh (safety net)
.email_*.json
